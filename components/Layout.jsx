import {Home,Database,Trophy} from 'lucide-react';
export default function Layout({page,setPage,children}){const nav=[['home','Trang chủ',Home],['database','Kho Item',Database],['arena','Khu thi đấu',Trophy]];return <div className="app"><header className="topbar"><button className="brand" onClick={()=>setPage('home')}><span className="brand-mark">A</span><span>Aetheria</span></button><nav>{nav.map(([id,label,Icon])=><button key={id} className={page===id?'active':''} onClick={()=>setPage(id)}><Icon size={17}/>{label}</button>)}</nav></header><main>{children}</main><footer>Aetheria • Fanmade • Dữ liệu tham khảo cộng đồng và nguồn công khai</footer></div>}